<!DOCTYPE html>
<html>
  <head>
    <%= render "shared/head" %>
  </head>

  <body class="<%= body_class(yield(:body_class)) %>">
    <%= render "shared/flash" %>
    <div class="">
      <%= link_to image_tag("UXZero-240x60.svg", class: "nav__logo mx-auto pt-10 w-48"), root_path %>
      <%= yield %>
    </div>
  </body>
</html>
